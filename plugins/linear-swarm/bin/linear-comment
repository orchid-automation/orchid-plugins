#!/usr/bin/env bash
# linear-comment compatibility wrapper — delegates to the shared linear-swarm runtime

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
SHARED_ROOT="$(cd "${SCRIPT_DIR}/../../_shared/linear-swarm" && pwd)"

exec "${SHARED_ROOT}/bin/linear-comment" "$@"
